// import EncryptedStorage from 'react-native-encrypted-storage';

// export const setEncryptedData = async (key, data) => {
//     try {
//       const res = await EncryptedStorage.setItem(key, data);
//       return res;
//       // Congrats! You've just stored your first value!
//     } catch (error) {
//       return null;
//       // There was an error on the native side
//     }
//   };

//   export const getEncryptedData = async key => {
//     try {
//       const res = await EncryptedStorage.getItem(key);
//       return res;
//     } catch (error) {
//       return null;
//       // There was an error on the native side
//     }
//   };
